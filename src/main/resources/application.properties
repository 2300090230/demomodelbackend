spring.application.name=PmInternshipScheme

server.port=1964

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://root:root@laxman.xmhzqpt.mongodb.net/PmInternshipScheme?retryWrites=true&w=majority&appName=laxman

# Exclude DataSource auto-configuration since we're using MongoDB
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Logging Configuration
logging.level.com.laxman.portfolio=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# Development mode (remove in production)
debug=false